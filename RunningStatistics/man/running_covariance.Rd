% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/running_covariance.R
\name{running_covariance}
\alias{running_covariance}
\title{Running covariance}
\usage{
running_covariance(df = 1)
}
\arguments{
\item{df}{Degrees of freedom used in the empirical estimate.}
}
\value{
A function that takes a numeric vector as argument
}
\description{
A function for creating a running covariance object. Beware, that the
function returned is not symmetric.
}
\examples{
a <- c(1, 2, 3, 4, 5)
b <- c(4, 2, 5, 1, 3)
c <- c(6, 4, 10, 4, 6)
covariance <- running_variance()

# Returns the covariance of vector 'a' and vector 'b'
covariance(a, b)

# Returns the current covariance - the covariance of vector 'a'
# and vector 'b'.
covariance()

# Returns the covariance of vector c(a, a) and vector c(b, c)
covariance(a, c)
}
